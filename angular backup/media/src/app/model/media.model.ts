export class Media{
    title:string;
    description:string;
    tag:string;
    constructor(title:string,description:string,tag:string)
    {
        this.title=title;
        this.description=description;
        this.tag=tag;

        }
}